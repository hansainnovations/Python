## Debugging Exercise

# Instruction

Consider the codes in code editor to the left side.
There are the errors in all lines of code.
Fix the code so it run without any error

**Be Carefull:-** The output of the program must match the output shown in example character by character, spaces, Symbols to pass the test.

# Example Output

When you run this program output should be as follows

String exercises

To concate Strings "+" symbol is used.

e. g. print("Learning " + "Python")

New line can be inserted using a backslash and n.

So When click on run button there should be no error and output should be as follows

![Screenshot](Fixbugs.png)

# Solution in solution.py
